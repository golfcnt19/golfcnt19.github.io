# CLAUDE.md

บริบทของเว็บพอร์ตโฟลิโอ อ่านก่อนแก้อะไร

## นี่คืออะไร

เว็บพอร์ตโฟลิโอส่วนตัวของ Chanathip Jaidee (golfcnt19) — HTML/CSS/JS ล้วน ไม่มี framework ไม่มี build step
ออนไลน์ที่ **https://golfcnt19.github.io** ผ่าน GitHub Pages (branch `main`, root)

repo **ต้องชื่อ `golfcnt19.github.io`** เท่านั้น ถ้าเปลี่ยนชื่อ URL จะกลายเป็น subpath
แล้ว `canonical` กับ `og:url` ใน `index.html` จะผิด ต้องตามแก้

## กติกาที่พังง่ายที่สุด

**เนื้อหาทั้งหมดอยู่ใน [`js/content.js`](js/content.js) ไฟล์เดียว** ไม่ต้องแตะ HTML/CSS

**`th` กับ `en` ต้องมีคีย์ตรงกันและ array ยาวเท่ากัน** — ถ้าเพิ่มรายการฝั่งเดียว สลับภาษาแล้วเนื้อหาหาย

## พฤติกรรมที่ตั้งใจออกแบบ อย่าไปแก้

**section ที่ `items: []` จะซ่อนตัวเองทั้ง section พร้อมลิงก์ในเมนู** และลิงก์ที่ชี้ไปหามัน
(เช่นปุ่ม "ดูผลงาน" ใน hero) จะเบนไป section ถัดไปอัตโนมัติ พอเติมรายการกลับก็คืนค่าเอง

**เนื้อหามองเห็นได้เป็นค่าตั้งต้น** จะซ่อนเพื่อทำอนิเมชันก็ต่อเมื่อ JS ยืนยันแล้วว่ารองรับ
IntersectionObserver, ไม่ได้เปิด reduced-motion, และแท็บถูกเปิดดูอยู่จริง
ถ้ากลับด้านตรรกะนี้ เปิดในแท็บพื้นหลังจะได้หน้าว่างเปล่า

**ตัวเลขสถิติใส่ค่าจริงลง DOM ตั้งแต่แรก** การนับขึ้นเป็นของแถม ไม่ใช่แหล่งข้อมูล

## ⚠️ ข้อจำกัด NDA

**งานภายในธนาคารกรุงศรีเอามาใส่ไม่ได้เลย** เจ้าของยืนยันเอง

- **Krungsri iPro** เป็นผลิตภัณฑ์สาธารณะ ระบุชื่อได้ในระดับที่สาธารณะรู้ แต่ยังไม่ได้ใส่
- **Krungsri iSimple** ไม่พบว่าเป็นสาธารณะ — **ถือว่าภายใน อย่าใส่**
- ห้ามแตะ: สถาปัตยกรรมภายใน มาตรการความปลอดภัย ตัวเลขผู้ใช้/ธุรกรรม ชื่อ vendor

## ดูผลก่อน push

```bash
npx --yes serve . -l 4173
```

push ขึ้น `main` แล้วรอ 1–2 นาที Pages อัปเดตเอง

`.nojekyll` ห้ามลบ ไม่งั้นโฟลเดอร์ที่ขึ้นต้นด้วย `_` จะหายไป

## ที่ยังค้าง

- คำอธิบายงานที่กรุงศรีกับแสงชัยยังเป็นข้อความกลาง ๆ ที่ AI เขียนจากบริบท ไม่ใช่จากที่เจ้าของเล่า
  ตรงนี้คือจุดที่คนอ่านพอร์ตสนใจที่สุด
- `meta.links` LinkedIn ยังเว้นว่าง
- ปีที่เรียน ม.บูรพา ยังไม่ได้ใส่ (`period` เว้นว่างได้ บรรทัดจะไม่แสดง)
