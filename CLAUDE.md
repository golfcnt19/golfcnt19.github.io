# CLAUDE.md

บริบทของเว็บพอร์ตโฟลิโอ อ่านก่อนแก้อะไร

## นี่คืออะไร

เว็บพอร์ตโฟลิโอส่วนตัวของ Chanathip Jaidee (golfcnt19) — HTML/CSS/JS ล้วน ไม่มี framework ไม่มี build step
ออนไลน์ที่ **https://golfcnt19.github.io** ผ่าน GitHub Pages (branch `main`, root)

repo **ต้องชื่อ `golfcnt19.github.io`** เท่านั้น ถ้าเปลี่ยนชื่อ URL จะกลายเป็น subpath
แล้ว `canonical` กับ `og:url` ใน `index.html` จะผิด ต้องตามแก้

มีสองหน้า ใช้เนื้อหาชุดเดียวกัน

| ไฟล์ | คือ | สไตล์ | สคริปต์ |
|---|---|---|---|
| `index.html` | เว็บพอร์ต ธีมมืด | `css/site.css` | `js/site.js` |
| `resume.html` | เรซูเม่ A4 สองคอลัมน์ สำหรับพิมพ์ | `css/resume.css` | `js/resume.js` |

รูปถ่ายอยู่ที่ `image/chanathip-white.jpg` (720×1080, 87 KB) ใช้บนหน้าเรซูเม่อย่างเดียว
ชี้ผ่าน `meta.photo` ใน `content.js` — ลบคีย์นั้นหรือเว้นว่าง กรอบรูปจะยุบหายไปเอง ไม่พัง

**อย่าเอารูปต้นฉบับความละเอียดเต็มเข้า repo** ตัวที่อยู่ในนี้คือรูป ID ที่คีย์พื้นหลังฟ้า
ออกเป็นขาวและย่อแล้ว ต้นฉบับ 1200×1800 หนัก 930 KB ซึ่งใหญ่เกินไปสำหรับ repo ที่เสิร์ฟ
เป็นเว็บ และพอ push แล้วมันจะโหลดได้สาธารณะด้วย เก็บต้นฉบับไว้นอก repo

## กติกาที่พังง่ายที่สุด

**เนื้อหาทั้งหมดอยู่ใน [`js/content.js`](js/content.js) ไฟล์เดียว** ไม่ต้องแตะ HTML/CSS
ทั้งสองหน้าอ่านจากไฟล์นี้ไฟล์เดียวกัน แก้ทีเดียวเปลี่ยนทั้งคู่

**`th` กับ `en` ต้องมีคีย์ตรงกันและ array ยาวเท่ากัน** — ถ้าเพิ่มรายการฝั่งเดียว สลับภาษาแล้วเนื้อหาหาย
กติกานี้รวมถึงบล็อก `resume` ที่เพิ่มเข้ามาใหม่ด้วย

## พฤติกรรมที่ตั้งใจออกแบบ อย่าไปแก้

**section ที่ `items: []` จะซ่อนตัวเองทั้ง section พร้อมลิงก์ในเมนู** และลิงก์ที่ชี้ไปหามัน
(เช่นปุ่ม "ดูผลงาน" ใน hero) จะเบนไป section ถัดไปอัตโนมัติ พอเติมรายการกลับก็คืนค่าเอง

**เนื้อหามองเห็นได้เป็นค่าตั้งต้น** จะซ่อนเพื่อทำอนิเมชันก็ต่อเมื่อ JS ยืนยันแล้วว่ารองรับ
IntersectionObserver, ไม่ได้เปิด reduced-motion, และแท็บถูกเปิดดูอยู่จริง
ถ้ากลับด้านตรรกะนี้ เปิดในแท็บพื้นหลังจะได้หน้าว่างเปล่า

**ตัวเลขสถิติใส่ค่าจริงลง DOM ตั้งแต่แรก** การนับขึ้นเป็นของแถม ไม่ใช่แหล่งข้อมูล

**ปุ่ม Résumé พาไปที่ [`resume.html`](resume.html) ไม่ได้ดาวน์โหลดไฟล์ที่เก็บไว้**
หน้านั้นเป็นเรซูเม่สองคอลัมน์ที่จัดมาให้พอดี A4 หนึ่งหน้า กดปุ่ม "ดาวน์โหลด PDF"
แล้วเลือกปลายทางเป็น "Save as PDF" ในหน้าต่างพิมพ์ — **ไม่มีไฟล์ PDF เก็บใน repo**
เพราะมันจะกลายเป็นแหล่งข้อมูลชุดที่สองที่ต้องตามแก้ทุกครั้ง และต้องมีสองไฟล์เพราะสองภาษา
ภาษาไทยเรนเดอร์ถูกเสมอเพราะเบราว์เซอร์วาดเอง ไม่ต้องฝังฟอนต์เหมือนไลบรารีสร้าง PDF

**เรซูเม่พอดีหนึ่งหน้าแบบเฉียดฉิว — เหลือที่ว่างแค่ ~20px** วัดที่ 96dpi (หน้า A4 = 1123px)
ตอนนี้คอลัมน์ซ้าย ~1103px คอลัมน์ขวา ~1083px (อังกฤษ) **ตึงทั้งคู่**
เพิ่มอะไรก็ตามต้องวัดซ้ำ ไม่งั้นล้นไปหน้าสองโดยไม่รู้ตัว

ตัวถ่วงของแต่ละฝั่ง — ซ้าย: `skills.groups` (9 กลุ่ม กินครึ่งคอลัมน์)
ขวา: `about.body`, `experience.items[].points` และ `resume.howPoints`

**ต้องวัดที่ viewport กว้าง ≥ 900px เท่านั้น** ถ้าวัดตอนหน้าต่างแคบ กฎ `screen` ของจอมือถือ
จะทำงานแล้วได้ตัวเลขที่ไม่ตรงกับกระดาษ วิธีจำลองสภาพพิมพ์คือฉีดกฎในบล็อก `@media print`
เข้ามาเป็น screen แล้วล็อก `.sheet { width: 210mm }` (บล็อก print ตั้ง `width: auto` ไว้)

**อย่าใช้ `requestAnimationFrame` ตอนวัดผ่าน Browser pane** ถ้า pane ถูกซ่อน เบราว์เซอร์
หยุดวาดเฟรม rAF จะไม่ยิงแล้วค้างยาว ใช้ `void document.body.offsetHeight` บังคับ layout แทน
และใช้ `XMLHttpRequest` แบบ sync แทน `fetch` + `await`

**เรซูเม่ไม่มี section ผลงานและบุคคลอ้างอิง** ถอดออกจาก `resume.html` แล้ว ไม่ได้แค่ซ่อน
ผลงานให้ดูจากลิงก์ `golfcnt19.github.io` กับ GitHub ที่อยู่ในหัวข้อ CONTACT คอลัมน์ซ้าย
อย่าเติมบรรทัดลิงก์ซ้ำที่ท้ายคอลัมน์ขวาอีก มันซ้ำกับที่มีอยู่แล้ว

คอลัมน์ขวาปิดท้ายด้วย **วิธีทำงาน** (`resume.howPoints`) แทน — 4 ข้อ
**ต้องสั้นพอให้ลงบรรทัดเดียวทุกข้อ** ที่ความกว้างคอลัมน์ ~541px ขนาด 9.6pt
ตกไปสองบรรทัดเมื่อไหร่คือเกินหน้าทันที ภาษาอังกฤษยาวประมาณ 70 ตัวอักษรคือเพดาน

**งานแต่ละที่มีสองเวอร์ชัน** `desc` เป็นย่อหน้าสำหรับไทม์ไลน์บนเว็บ ส่วน `points`
เป็นหัวข้อย่อยสำหรับเรซูเม่ — `resume.js` จะหยิบ `points` ก่อนถ้ามี ถ้าไม่มีค่อยใช้ `desc`
แก้อันหนึ่งแล้วอย่าลืมดูอีกอันว่ายังตรงกันอยู่ไหม

วิธีวัดเร็ว ๆ เปิด `resume.html` แล้วรันใน console

```js
[...document.querySelectorAll('.rail,.body')].map(el => {
  const vis = [...el.children].filter(c => c.getBoundingClientRect().height > 0);
  const bottom = Math.max(...vis.map(c => c.getBoundingClientRect().bottom));
  return Math.round(bottom - el.getBoundingClientRect().top +
    parseFloat(getComputedStyle(el).paddingBottom));
});  // ทั้งคู่ต้องไม่เกิน 1123
```

`.rail-bg` เป็น element เปล่า ๆ ที่แยกออกมาโดยตั้งใจ ตอนพิมพ์มันกลายเป็น `position: fixed`
เพื่อให้แถบสีลากต่อทุกหน้าถ้าเนื้อหาล้น — ถ้ายุบมันกลับไปเป็น `background` ของ `.rail`
แถบสีจะหายตั้งแต่หน้าสองเป็นต้นไป

**พื้นหลังเข้มอาจไม่ออกมาบนกระดาษ** Chrome มีช่อง "Background graphics" ที่ปิดไว้เป็นค่าตั้งต้น
`print-color-adjust: exact` ใส่ไว้แล้วแต่บางเวอร์ชันไม่สนใจ ถ้าแถบซ้ายออกมาขาวให้ติ๊กช่องนั้น

**⚠️ media query ทุกอันที่เป็น `max-width` ต้องขึ้นต้นด้วย `screen and`**
กระดาษ A4 ที่ 96dpi กว้าง **794px** ถ้าเขียนแค่ `@media (max-width: 900px)` กฎจอมือถือ
จะไปทับตอนพิมพ์ด้วย แล้วเลย์เอาต์บนกระดาษกลายเป็นแบบมือถือเงียบ ๆ โดยที่พรีวิวบนจอดูปกติ
กฎนี้ใช้กับทั้ง `resume.css` และ `site.css`

วิธีตรวจว่าไม่มีตัวไหนหลุด รันใน console

```js
[...document.styleSheets].flatMap(s => [...s.cssRules])
  .filter(r => r.type === CSSRule.MEDIA_RULE)
  .map(r => r.media.mediaText)
  .filter(m => /max-width/.test(m) && !/screen/.test(m));   // ต้องได้ []
```

`resume.html` ใช้ `localStorage` คีย์ `portfolio.lang` ตัวเดียวกับเว็บหลัก เลือกภาษาแล้วจำข้ามหน้า

## ⚠️ ข้อจำกัด NDA

**งานภายในธนาคารกรุงศรีเอามาใส่ไม่ได้เลย** เจ้าของยืนยันเอง

- **Krungsri iPro** เป็นผลิตภัณฑ์สาธารณะ ระบุชื่อได้ในระดับที่สาธารณะรู้ แต่ยังไม่ได้ใส่
- **Krungsri iSimple** ไม่พบว่าเป็นสาธารณะ — **ถือว่าภายใน อย่าใส่**
- ห้ามแตะ: สถาปัตยกรรมภายใน มาตรการความปลอดภัย ตัวเลขผู้ใช้/ธุรกรรม ชื่อ vendor

## ดูผลก่อน push

```bash
npx --yes serve . -l 4173
```

push ขึ้น `main` แล้วรอ 1–2 นาที Pages อัปเดตเอง

`.nojekyll` ห้ามลบ ไม่งั้นโฟลเดอร์ที่ขึ้นต้นด้วย `_` จะหายไป

## ที่ยังค้าง

- คำอธิบายงานที่กรุงศรีกับแสงชัยยังเป็นข้อความกลาง ๆ ที่ AI เขียนจากบริบท ไม่ใช่จากที่เจ้าของเล่า
  ตรงนี้คือจุดที่คนอ่านพอร์ตสนใจที่สุด
- `meta.links` LinkedIn ยังเว้นว่าง
- ปีที่เรียน ม.บูรพา ยังไม่ได้ใส่ (`period` เว้นว่างได้ บรรทัดจะไม่แสดง)
